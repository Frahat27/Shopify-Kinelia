---
type: quick
task_id: 260911-tt4
files_modified:
  - sections/announcement-bar.liquid
  - sections/footer.liquid
autonomous: true
---

<objective>
Fix the CI-failing Theme Check rule `StaticStylesheetAndJavascriptTags` ("Liquid
tag found in CSS block. {% stylesheet %} tags should only contain static CSS
code.") in two files by converting the two offending Liquid comment tags that
sit *inside* `{% stylesheet %}` blocks into native CSS block comments. This was
caught by GitHub Actions run 34661542595, job "Theme Check (gate)" — the
authoritative required-status-check gate defined in
`.github/workflows/ci.yml` and mirrored locally by `npm run lint`
(`package.json`).

Purpose: `sections/announcement-bar.liquid` (lines 49-54) and
`sections/footer.liquid` (lines 152-166) each carry a
`{% comment %} ... {% endcomment %}` block placed directly inside their
section's `{% stylesheet %}...{% endstylesheet %}` tag, documenting real
project decisions (D-11/D-12/D-15, z-index layering order, the
`has-sticky-atc` coexistence contract owed to Phase 6). `{% stylesheet %}`
content is meant to be 100% static CSS — a Liquid comment tag inside it is
exactly the "Liquid tag found in CSS block" the rule flags. The fix must keep
every word of that documentation intact; only the comment *delimiter syntax*
changes, from Liquid's `{% comment %}` / `{% endcomment %}` to CSS's `/*` /
`*/`. Two other `{% comment %}` blocks exist in `footer.liquid` (lines 1-22
and 96-104) but sit *outside* the `{% stylesheet %}` tag — those are valid
Liquid comments today and must NOT be touched.

Output: both files still render identical CSS/markup and identical
documentation content; `shopify theme check --fail-level error` reports zero
`StaticStylesheetAndJavascriptTags` offenses on these two files (pre-existing,
unrelated `ValidSchema` offenses from the sandbox's lack of network access to
`raw.githubusercontent.com` — documented in `.planning/STATE.md` under Phase 3
Blockers/Concerns — are not this task's concern and do not block it).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@sections/announcement-bar.liquid
@sections/footer.liquid
@.github/workflows/ci.yml
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert the sticky-bar Liquid comment inside announcement-bar.liquid's stylesheet block to a CSS comment</name>
  <files>sections/announcement-bar.liquid</files>
  <action>
Open sections/announcement-bar.liquid. Its `{% stylesheet %}...{% endstylesheet %}`
tag spans lines 25-62. Inside it, lines 49-54 are a `{% comment %}` /
`{% endcomment %}` block documenting the phone-only sticky positioning
decision (plan 03-03, D-12) and the `--z-sticky-header` stacking rationale —
this is exactly what CI's `StaticStylesheetAndJavascriptTags` rule flags at
"error around line 49". Replace only the opening `{% comment %}` tag (line 49)
with `/*` and the closing `{% endcomment %}` tag (line 54) with `*/`, keeping
each on its own line at the same indentation as today. The four documentation
lines in between (the plan 03-03 / D-12 sticky-phone-only explanation and the
`--z-sticky-header` layering note) must stay byte-for-byte identical —
same words, same line breaks, same indentation — nothing shortened,
paraphrased, or reworded. Do not touch any other line in the file: the
`{%- comment -%}` block at the top of the file (lines 1-11, outside the
stylesheet tag) is a different, valid Liquid comment and must be left exactly
as-is; the rest of the stylesheet rules, the schema block, and the markup
above are unaffected.
  </action>
  <verify>
    <automated>cd "C:/Users/LENOVO/Documents/5- REPOS CRUSOR/Kinelia/SHOPIFY KInelia/Shopify-Kinelia" &amp;&amp; STRAY=$(sed -n '/{% *stylesheet *%}/,/{% *endstylesheet *%}/p' sections/announcement-bar.liquid | sed '1d;$d' | grep -n '{%'); if [ -n "$STRAY" ]; then echo "$STRAY"; exit 1; else echo PASS; fi</automated>
  </verify>
  <done>
Lines 49 and 54 (or their new positions after the edit) read `/*` and `*/`
respectively instead of the Liquid comment tag pair; the four lines of
documentation text between them are unchanged verbatim. Scanning the
interior of the file's `{% stylesheet %}...{% endstylesheet %}` region (every
line strictly between the opening and closing stylesheet tags) finds zero
occurrences of the two-character sequence that opens a Liquid tag — i.e. the
automated verify command above prints `PASS`. `git diff` on the file shows
only the two delimiter-line changes, no other lines touched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert the WhatsApp-FAB Liquid comment inside footer.liquid's stylesheet block to a CSS comment</name>
  <files>sections/footer.liquid</files>
  <action>
Open sections/footer.liquid. Its second `{% stylesheet %}...{% endstylesheet %}`
tag spans lines 105-191. Inside it, lines 152-166 are a `{% comment %}` /
`{% endcomment %}` block documenting the floating WhatsApp button (plan 03-04,
SHELL-02/D-15), the `--z-fab` stacking order, and the `has-sticky-atc`
coexistence rule owed to Phase 6 — this is the second location CI's
`StaticStylesheetAndJavascriptTags` rule flags, "error around line 152".
Replace only the opening `{% comment %}` tag (line 152) with `/*` and the
closing `{% endcomment %}` tag (line 166) with `*/`, each on its own line at
the same indentation as today.

Every content line between those two delimiters must stay byte-for-byte
identical, with exactly one deliberate, narrowly-scoped exception forced by
this fix: the sentence "y una section puede llevar un {% stylesheet %} scoped
que un layout no puede" quotes the literal `{% stylesheet %}` tag syntax as
prose. Today that literal sits safely inert because `{% comment %}` content is
a Liquid raw block that Liquid never tokenizes. Once you remove that
`{% comment %}` wrapper, the same text becomes plain content directly inside
the *outer* `{% stylesheet %}...{% endstylesheet %}` tag — a region Liquid
and Theme Check DO tokenize for nested tags (that tokenization is the whole
reason the original `{% comment %}` block was detected as an offense). Leaving
the literal `{% stylesheet %}` text in place would either break Liquid's
parse of the file or immediately reproduce the exact same
`StaticStylesheetAndJavascriptTags` offense inside the new CSS comment. Remove
only the four decorative characters — the opening two and the closing two —
immediately wrapping the word "stylesheet" in that one sentence, so it reads
"y una section puede llevar un stylesheet scoped que un layout no puede". Do
not change any other word, space, or punctuation in that sentence, and do not
touch any other content line in the block.

Do NOT touch the other two `{% comment %}` / `{% endcomment %}` blocks in this
file: the file-header block (lines 1-22) and the block documenting
`--color-text` and the footer layout breakpoint (lines 96-104). Both sit
outside the `{% stylesheet %}` tag and are valid Liquid comments today — leave
them exactly as they are.
  </action>
  <verify>
    <automated>cd "C:/Users/LENOVO/Documents/5- REPOS CRUSOR/Kinelia/SHOPIFY KInelia/Shopify-Kinelia" &amp;&amp; STRAY=$(sed -n '/{% *stylesheet *%}/,/{% *endstylesheet *%}/p' sections/footer.liquid | sed '1d;$d' | grep -n '{%'); if [ -n "$STRAY" ]; then echo "$STRAY"; exit 1; else echo PASS; fi</automated>
  </verify>
  <done>
Lines 152 and 166 (or their new positions after the edit) read `/*` and `*/`
respectively instead of the Liquid comment tag pair. Every content line
between them is unchanged verbatim except the one deliberate, documented
exception: the sentence about a section carrying its own scoped stylesheet no
longer contains the literal `{%`/`%}` characters around the word "stylesheet"
(it now reads "un stylesheet scoped" instead of "un {% stylesheet %} scoped"),
with every other word of that sentence intact. The two other
`{% comment %}` blocks in the file (lines 1-22 and 96-104) are byte-for-byte
unchanged. Scanning the interior of the file's second
`{% stylesheet %}...{% endstylesheet %}` region finds zero occurrences of the
two-character sequence that opens a Liquid tag — i.e. the automated verify
command above prints `PASS`. `git diff` on the file shows only the two
delimiter-line changes and the one four-character removal, no other lines
touched.
  </done>
</task>

</tasks>

<verification>
Both tasks' automated `<verify>` commands are network-independent and
authoritative for this fix: each greps the interior of the edited file's
`{% stylesheet %}...{% endstylesheet %}` span (excluding the tag lines
themselves) for any Liquid tag-opening sequence and fails if one remains.
Run both, then additionally attempt the real repo-standard command per the
task's own requirement — the same command CI's "Theme Check (gate)" step and
`package.json`'s `lint` script both run — and confirm it reports zero
`StaticStylesheetAndJavascriptTags` offenses (a failure to execute at all, or
unrelated `ValidSchema` offenses from the sandbox's lack of network access to
`raw.githubusercontent.com`, are pre-existing per `.planning/STATE.md` and do
NOT block this task):

    cd "C:/Users/LENOVO/Documents/5- REPOS CRUSOR/Kinelia/SHOPIFY KInelia/Shopify-Kinelia"
    for f in sections/announcement-bar.liquid sections/footer.liquid; do
      echo "--- $f ---"
      STRAY=$(sed -n '/{% *stylesheet *%}/,/{% *endstylesheet *%}/p' "$f" | sed '1d;$d' | grep -n '{%')
      if [ -n "$STRAY" ]; then echo "FAIL: $STRAY"; exit 1; fi
      echo "OK: no stray Liquid tags inside the stylesheet block"
    done
    if command -v shopify >/dev/null 2>&1; then
      OUT=$(shopify theme check --fail-level error 2>&1); CODE=$?
    else
      OUT="shopify CLI not runnable in this sandbox (no network / incomplete global install per STATE.md known gap)"
      CODE=127
    fi
    echo "shopify theme check exit code: $CODE"
    echo "$OUT"
    RULE_COUNT=$(printf '%s\n' "$OUT" | grep -c "StaticStylesheetAndJavascriptTags")
    echo "StaticStylesheetAndJavascriptTags offenses reported: $RULE_COUNT"
    if [ "$RULE_COUNT" -gt 0 ]; then echo "FAIL: rule still firing"; exit 1; fi
    echo "PASS: zero StaticStylesheetAndJavascriptTags offenses"

If the live `shopify theme check` run cannot execute at all in this sandbox
(confirmed during planning: the global `@shopify/cli` install here is
incomplete — no `bin/run.js`, no `package.json` under
`node_modules/@shopify/cli` — so `shopify`/`npx shopify` cannot run without
network access to reinstall it), the two static per-file checks above are
the authoritative pass/fail signal for this task. Note in the SUMMARY.md
whether the live CLI check executed, and if not, that a full `npm run lint`
run in CI or a networked environment remains the final confirmation (already
a standing note in `.planning/STATE.md` for Phase 3).
</verification>

<success_criteria>
- `sections/announcement-bar.liquid` lines 49/54 use `/*`/`*/` instead of
  `{% comment %}`/`{% endcomment %}`; the four documentation lines between
  them are unchanged verbatim.
- `sections/footer.liquid` lines 152/166 use `/*`/`*/` instead of
  `{% comment %}`/`{% endcomment %}`; all content lines between them are
  unchanged verbatim except the one narrow, documented removal of the `{%`/
  `%}` decoration around the word "stylesheet" in one sentence.
- The other two `{% comment %}` blocks in `footer.liquid` (outside its
  `{% stylesheet %}` tag) are untouched.
- Both per-task automated static-grep verifies print `PASS`.
- The phase-level `<verification>` block's best-effort live `shopify theme
  check --fail-level error` run (or its documented inability to execute in
  this sandbox) confirms zero `StaticStylesheetAndJavascriptTags` offenses;
  pre-existing, unrelated `ValidSchema` offenses do not block this task.
- No other lines in either file were modified (`git diff` scoped to exactly
  the described changes).
</success_criteria>

<output>
Create `.planning/quick/260911-tt4-fix-theme-check-staticstylesheetandjavas/260911-tt4-SUMMARY.md` when done.
</output>

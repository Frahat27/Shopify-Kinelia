---
type: quick
task_id: 260911-f2e
files_modified:
  - .planning/phases/03-layout-shell-seams-de-etapa-2/COVERAGE.md
autonomous: true
---

<objective>
Add a parseable API-coverage matrix table to Phase 3's COVERAGE.md so the
`api-coverage.verify-pre` gate parses a valid, non-empty matrix instead of
falling through to the empty-matrix error.

Purpose: `.planning/phases/03-layout-shell-seams-de-etapa-2/COVERAGE.md`
currently only contains a narrative "Declaración" section (a blockquoted
`> No external API integration: ...` line, which the parser's
`NO_INTEGRATION_DECLARATION_RE` deliberately does NOT match because it is
inside a blockquote) plus supporting evidence prose. `parseCoverageMatrix()`
finds neither a declaration nor a markdown table, so `validateCoverageMatrix()`
returns `rows.length === 0` and the seal-time gate blocks with "matrix is
empty — no capabilities enumerated". The fix is additive: append a real
`| capability | decision | reason |` table (the canonical format defined in
`gsd-core/bin/lib/api-coverage.cjs` lines 38-40, 634-747) describing the one
capability this phase actually touches — the future `/collect` endpoint,
which Phase 3 only wires as no-op Etapa 2 seams — as an `OPT-OUT` row with a
concrete, non-empty reason. All existing narrative content stays untouched as
supporting evidence.

Output: `.planning/phases/03-layout-shell-seams-de-etapa-2/COVERAGE.md` with
the existing "Declaración" / "Evidencia de respaldo" / "Cuándo cambia esto"
sections preserved verbatim, plus a new matrix section the gate can parse.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-layout-shell-seams-de-etapa-2/COVERAGE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert API-coverage matrix table into COVERAGE.md</name>
  <files>.planning/phases/03-layout-shell-seams-de-etapa-2/COVERAGE.md</files>
  <action>
Edit `.planning/phases/03-layout-shell-seams-de-etapa-2/COVERAGE.md`. Insert a
new `## Matriz de cobertura de API` section immediately after the existing
`## Declaración` section's blockquote paragraph (i.e., before the
`## Evidencia de respaldo (un revisor puede verificarla)` heading). Do NOT
remove, reword, or reorder any existing heading or paragraph — this is a pure
addition. The new section must contain exactly this table (verbatim, so the
parser in `gsd-core/bin/lib/api-coverage.cjs` recognizes the header via
`cleaned[0].toLowerCase() === 'capability'`, the separator row via the
`/^:?-{3,}:?$/` dash-run check, and the decision cell via the
`VALID_DECISIONS` set `{INTEGRATE, OPT-OUT}`):

| capability | decision | reason |
|---|---|---|
| Etapa 2 /collect instrumentation (no-op seams) | OPT-OUT | Fase 3 solo define seams no-op (script de atribución comentado, bus de eventos DOM, contrato ETAPA-2-SEAMS.md); ningún request sale hacia /collect ni ningún host hasta que Etapa 2 los active. |

Keep the table as its own top-level markdown table (blank line before and
after) so no unrelated line accidentally continues the `inMatrix` scan state
in the parser. Do not add any other row, do not invent a second capability
not already named in the file's existing "Declaración" text, and do not touch
the blockquoted `> No external API integration: ...` line — it stays exactly
as-is (it is intentionally NOT what the gate parses; the new table is).
  </action>
  <verify>
    <automated>node "C:/Users/LENOVO/.claude/gsd-core/bin/gsd-tools.cjs" check api-coverage.verify-pre "C:/Users/LENOVO/Documents/5- REPOS CRUSOR/Kinelia/SHOPIFY KInelia/Shopify-Kinelia/.planning/phases/03-layout-shell-seams-de-etapa-2" --raw</automated>
  </verify>
  <done>
Running the automated verify command above prints JSON whose `block` field is
`false` (not `true`). The existing "Declaración", "Evidencia de respaldo" and
"Cuándo cambia esto" sections are byte-identical to before this task except
for the one new "## Matriz de cobertura de API" section inserted between
"Declaración" and "Evidencia de respaldo". `git diff` on the file shows only
additions (no deletions of existing lines).
  </done>
</task>

</tasks>

<verification>
Run the exact command from Task 1's `<automated>` verify step:

```
node "C:/Users/LENOVO/.claude/gsd-core/bin/gsd-tools.cjs" check api-coverage.verify-pre "C:/Users/LENOVO/Documents/5- REPOS CRUSOR/Kinelia/SHOPIFY KInelia/Shopify-Kinelia/.planning/phases/03-layout-shell-seams-de-etapa-2" --raw
```

Confirm the JSON output has `"block": false`. If it still reports `block: true`,
re-inspect the table for a malformed separator row, a decision value outside
`{INTEGRATE, OPT-OUT}`, or a missing reason on the `OPT-OUT` row — the parser
errors surface directly in the JSON `errors`/`reasons` field.
</verification>

<success_criteria>
- `.planning/phases/03-layout-shell-seams-de-etapa-2/COVERAGE.md` contains a
  parseable `| capability | decision | reason |` table with at least one row
- The row's decision is `OPT-OUT` with a non-empty reason grounded in the
  file's existing "Declaración" content (Etapa 2 `/collect` seams, not a
  fabricated capability)
- `api-coverage.verify-pre` on the phase directory reports `"block": false`
- All pre-existing narrative sections in COVERAGE.md are unchanged
</success_criteria>

<output>
Create `.planning/quick/260911-f2e-add-api-coverage-matrix-table-to-phase-3/260911-f2e-SUMMARY.md` when done.
</output>
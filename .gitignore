# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
node_modules/

# Shopify CLI files
.shopify/

## Release files
release
*.zip

# Environment files / secrets (lands before any credential exists — the only free time)
.env
.env.*
!.env.example

# Local-only overrides
*.local

# Brand source material (heavy binaries — the theme vendors the specific optimized
# files it needs into assets/, it does not carry the source folders)
*.pdf
brand/
Kinelia Brand Book.pdf
A- Logo/
A-Logos/

# Lighthouse CI output
.lighthouseci/

# GSD runtime locks (transient session/phase claims — never committed)
.planning/*.lock
.planning/milestone.lock
.gsd/
